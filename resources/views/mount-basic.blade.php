<x-layout>

    <div class="flex justify-center">
        So basically we have a mount method in the Livewire PHP Class file that's similar to a
        constructor. we must pass this argument to the component (< livewire:example :search=""/ >).
    </div>

    <livewire:mount-basic search="K"/>
</x-layout>
